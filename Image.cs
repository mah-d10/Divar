namespace Divar
{
    public class Image
    {
        private string Alt;
        private string Url;

        public Image(string alt, string url)
        {
            Alt = alt;
            Url = url;
        }
    }
}
